export function getLandingHeaderHTML(): string {
  return `
    <div class="landing-header">
      <div class="landing-logo">ℕ</div>
      <h1 class="landing-title">Numericle</h1>
      <p class="landing-subtitle">Discover the hidden sequence rule</p>
    </div>
  `;
}
